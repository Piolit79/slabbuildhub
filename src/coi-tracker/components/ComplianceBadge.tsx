import { COI } from '@/coi-tracker/types';

// ComplianceBadge removed — compliance details shown inline in the COI detail dialog
export function ComplianceBadge({ coi }: { coi: COI; className?: string }) {
  return null;
}
